# Optional: only needed for server-side calls. Browser uses /api → Next rewrites to Express.
# NEXT_PUBLIC_API_URL=http://127.0.0.1:4000
# If Express is not on 4000, set for the dev proxy:
# API_PROXY_TARGET=http://127.0.0.1:4000
