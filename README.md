# Office Portal

Employee management, geofenced attendance, configurable payroll rules, leaves, payslips, and role-based dashboards.

See **[SETUP.md](./SETUP.md)** for installation, environment variables, API overview, and database schema.

**Security reminder:** keep MongoDB credentials and `JWT_SECRET` in local `.env` files only — never commit secrets.
